# Credentials — OAuth tokens / refresh tokens
agent/auth.json
.env
.env.local
*.pem
*.key

# Conversation history (contains project contents and prompts)
agent/sessions/

# Fetched provider catalog cache (etag/timestamps, regenerated on launch)
agent/models-store.json

# Local runtime data, caches, and settings backups
agent/fff/
agent/run-history.jsonl
agent/backups/
pi-acp/session-map.json
web-search-cache/

# Vendored binaries
agent/bin/

# Dependencies
node_modules/

# OS / editor noise
.DS_Store
*.swp
*.log
