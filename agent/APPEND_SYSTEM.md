# IMPORTANT

- Use ask_user tool when making any decisions or clarifications are needed. Don't make any decisions or assumptions without asking the user first
- Don't commit any changes unless explicitly asked to
- Use en dashes (`-`) instead of em dashes (`—`)
