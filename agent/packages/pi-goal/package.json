{
  "name": "pi-goal-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Approval-first goal workflow for Pi TUI and Zed through pi-acp",
  "keywords": ["pi-package"],
  "pi": { "extensions": ["./index.ts"] },
  "scripts": { "test": "node --test test/*.test.mjs" },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
